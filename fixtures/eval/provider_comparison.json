[
  {
    "id": "explain-inline-completion",
    "intent": "Explain what the complete() function does in this file",
    "workspace_files": {
      "inline_completion.zig": "pub fn complete(allocator: std.mem.Allocator, request: CompletionRequest) !CompletionResult { var provider = try provider_factory.create(allocator, request.provider_options); defer provider.deinit(allocator); const prompt = try buildPrompt(allocator, request); defer allocator.free(prompt); var response_writer = std.Io.Writer.Allocating.init(allocator); defer response_writer.deinit(); try provider.ask(allocator, prompt, &.{}, &response_writer.writer); const text = response_writer.written(); return .{ .text = try allocator.dupe(u8, text) }; }"
    },
    "expect": {
      "path": "inline_completion.zig",
      "contains": "complete",
      "min_steps": 1
    }
  },
  {
    "id": "add-doc-comment",
    "intent": "Add a doc comment to the complete function",
    "workspace_files": {
      "inline_completion.zig": "pub fn complete(allocator: std.mem.Allocator, request: CompletionRequest) !CompletionResult { var provider = try provider_factory.create(allocator, request.provider_options); defer provider.deinit(allocator); const prompt = try buildPrompt(allocator, request); defer allocator.free(prompt); var response_writer = std.Io.Writer.Allocating.init(allocator); defer response_writer.deinit(); try provider.ask(allocator, prompt, &.{}, &response_writer.writer); const text = response_writer.written(); return .{ .text = try allocator.dupe(u8, text) }; }"
    },
    "expect": {
      "path": "inline_completion.zig",
      "contains": "///",
      "min_steps": 1
    }
  },
  {
    "id": "add-error-handling",
    "intent": "Add error handling to the complete function - return NoCompletion error if text is empty",
    "workspace_files": {
      "inline_completion.zig": "pub fn complete(allocator: std.mem.Allocator, request: CompletionRequest) !CompletionResult { var provider = try provider_factory.create(allocator, request.provider_options); defer provider.deinit(allocator); const prompt = try buildPrompt(allocator, request); defer allocator.free(prompt); var response_writer = std.Io.Writer.Allocating.init(allocator); defer response_writer.deinit(); try provider.ask(allocator, prompt, &.{}, &response_writer.writer); const text = response_writer.written(); return .{ .text = try allocator.dupe(u8, text) }; }"
    },
    "expect": {
      "path": "inline_completion.zig",
      "contains": "NoCompletion",
      "min_steps": 1
    }
  },
  {
    "id": "rename-function",
    "intent": "Rename the function complete to completeInline",
    "workspace_files": {
      "inline_completion.zig": "pub fn complete(allocator: std.mem.Allocator) void { }"
    },
    "expect": {
      "path": "inline_completion.zig",
      "contains": "completeInline",
      "min_steps": 1
    }
  },
  {
    "id": "add-test",
    "intent": "Add a unit test for the complete function that verifies it returns a result",
    "workspace_files": {
      "inline_completion.zig": "pub fn complete(allocator: std.mem.Allocator) ![]u8 { return try allocator.dupe(u8, \"test\"); }"
    },
    "expect": {
      "path": "inline_completion.zig",
      "contains": "test",
      "min_steps": 1
    }
  },
  {
    "id": "extract-helper",
    "intent": "Extract the prompt building logic into a separate function called buildCompletionPrompt",
    "workspace_files": {
      "inline_completion.zig": "pub fn complete(allocator: std.mem.Allocator, request: CompletionRequest) !CompletionResult { const prompt = try std.fmt.allocPrint(allocator, \"Complete: {s}\", .{request.prefix}); defer allocator.free(prompt); return .{ .text = try allocator.dupe(u8, prompt) }; }"
    },
    "expect": {
      "path": "inline_completion.zig",
      "contains": "buildCompletionPrompt",
      "min_steps": 1
    }
  }
]
