.zig-cache/
zig-out/
zig-pkg
.DS_Store
fixtures/eval-workspace/
/extension.o
/main.wasm
.forge/evals/
.forge/
fixtures/**/.forge/
test_map
scripts/__pycache__/

.env

# Python MCP Server
packages/mcp-computer/venv/
packages/mcp-computer/__pycache__/
.build/
test.db

# Binaries and assets
bin/
cursor.png

# Configuration
.mcp.json

# npm distribution — platform binaries are built per-platform and should
# not be committed. The npm/ source (JS shim, package.json, scripts) IS
# committed; only the built `forge` / `forge.exe` binaries are ignored.
npm/**/forge
npm/**/forge.exe
npm/**/*.tgz
