# Personal Extensions

Place personal Forge extensions here (not committed to git).

Each subfolder needs a `forge.toml` with an `[extension]` section. Reload from the Extensions sidebar or **Cmd+Shift+P → Reload Extensions**.

Example layout:

```
.forge/extensions/
  my-tool/
    forge.toml
```
